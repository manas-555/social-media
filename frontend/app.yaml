runtime: nodejs22

env: flex

handlers:
  - url: /static
    static_dir: build
  - url: /
    static_files: build/index.html
    upload: build/index.html
  - url: /(.*)
    static_files: build/\1
    upload: build/(.*)
